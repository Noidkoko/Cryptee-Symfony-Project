{% extends 'base.html.twig' %}

{% block title %}Welcome to Creeptee{% endblock %}

{% block content %}
            <main>
            <div class="hero-home">
                <h1>
                Welcome to Creeptee
                </h1>
                <h3>Play basics games, make bigger gains</h3>
            </div>
            {# <div class="games-container">
                {{ for game in games }}

                    <div class="game-case">
                        <img src="{{ path(game.url) }}" alt='game case'>
                        <h1>{{ game.name }}</h1>
                    </div>
            </div> #}
{% endblock %}